const styles = ({ spacing: { unit } }) => ({
  prestigeCell: {
    fontWeight: 'bold',
  },

  attributeCell: {
    borderBottom: 'none',
    padding: `${unit * 0.5}px ${unit}px`,
  },
});

export default styles;

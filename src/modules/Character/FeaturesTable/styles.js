const styles = ({ spacing }) => ({
  prestigeCell: {
    fontWeight: 'bold',
  },

  attributeCell: {
    borderBottom: 'none',
    padding: `${spacing(0.5)}px ${spacing()}px`,
  },
});

export default styles;
